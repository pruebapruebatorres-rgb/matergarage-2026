# URL base del backend (Java + Spring Boot). Ver ../backend.
# Local: http://localhost:8080
# En Vercel: se define como variable de entorno del proyecto (Settings → Environment
# Variables), no en este archivo — apuntando a la URL del servicio en Render
# (p. ej. https://mater-garage-api.onrender.com).
VITE_API_URL=http://localhost:8080
